<Window x:Class="RevitExporterAddin.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputDialog" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" x:Name="TitleText" 
                   FontSize="14" FontWeight="Bold" 
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center"/>
        
        <!-- Описание -->
        <TextBlock Grid.Row="1" x:Name="DescriptionText" 
                   FontSize="12" 
                   Margin="0,0,0,15"
                   HorizontalAlignment="Center"/>
        
        <!-- Поле ввода -->
        <TextBox Grid.Row="2" x:Name="InputTextBox" 
                 Height="30" 
                 FontSize="14"
                 Margin="0,0,0,15"/>
        
        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Отмена" 
                    Width="80" Height="30" Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="OkButton" Content="OK" 
                    Width="80" Height="30" 
                    Background="#007BFF" Foreground="White"
                    Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>


