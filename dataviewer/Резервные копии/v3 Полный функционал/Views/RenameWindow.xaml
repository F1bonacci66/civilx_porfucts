<Window x:Class="RevitExporterAddin.Views.RenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Переименовать" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Переименовать" 
                   FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- Название -->
        <TextBlock Grid.Row="1" Text="Новое название:" 
                   FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="NameTextBox" Grid.Row="2" 
                 Height="30" Margin="0,0,0,15"
                 FontSize="14"/>
        
        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CancelButton" Content="Отмена" 
                    Width="80" Height="30" Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="RenameButton" Content="Переименовать" 
                    Width="100" Height="30" 
                    Background="#007BFF" Foreground="White"
                    Click="RenameButton_Click"/>
        </StackPanel>
    </Grid>
</Window>

