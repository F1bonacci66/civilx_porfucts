<UserControl x:Class="RevitExporterAddin.Views.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Верхняя панель с вкладками -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E9ECEF" BorderThickness="0,0,0,1">
            <Grid Margin="20,20,20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Вкладки -->
                <TabControl x:Name="TabsControl" Grid.Column="0" 
                           BorderThickness="0" Background="Transparent">
                    <!-- Вкладки будут добавляться программно -->
                </TabControl>
                
                <!-- Кнопка добавления вкладки -->
                <Button x:Name="AddTabButton" Grid.Column="1" 
                        Content="+" Width="40" Height="30"
                        Background="#28A745" Foreground="White"
                        BorderThickness="0" Margin="10,0,0,0"
                        FontSize="18" FontWeight="Bold"
                        Click="AddTabButton_Click"/>
            </Grid>
        </Border>
        
        <!-- Основной контент -->
        <Border Grid.Row="1" Background="#F8F9FA">
            <Grid x:Name="TabContentArea">
                <!-- Здесь будет контент вкладки -->
            </Grid>
        </Border>
    </Grid>
</UserControl>

