<Window x:Class="RevitExporterAddin.TestWpfApp.AddExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить выгрузку" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Создать новую выгрузку" 
                   FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,20"
                   HorizontalAlignment="Center"/>
        
        <!-- Название выгрузки -->
        <TextBlock Grid.Row="1" Text="Название выгрузки:" 
                   FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="ExportNameTextBox" Grid.Row="2" 
                 Height="30" Margin="0,0,0,15"
                 FontSize="14"/>
        
        <!-- Путь к Revit файлу -->
        <TextBlock Grid.Row="3" Text="Путь к Revit файлу:" 
                   FontSize="14" Margin="0,0,0,5"/>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="RevitFilePathTextBox" Grid.Column="0" 
                     Height="30" Margin="0,0,10,0"
                     FontSize="14" IsReadOnly="True"/>
            <Button x:Name="BrowseButton" Grid.Column="1" 
                    Content="Обзор" Width="80" Height="30"
                    Click="BrowseButton_Click"/>
        </Grid>
        
        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CancelButton" Content="Отмена" 
                    Width="80" Height="30" Margin="0,0,10,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="CreateButton" Content="Создать" 
                    Width="80" Height="30" 
                    Background="#007BFF" Foreground="White"
                    Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
